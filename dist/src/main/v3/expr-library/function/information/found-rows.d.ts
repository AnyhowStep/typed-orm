import * as sd from "schema-decorator";
import { Expr } from "../../../expr";
export declare function foundRows(): (Expr<{
    usedColumns: never[];
    assertDelegate: sd.AssertDelegate<bigint>;
}>);
