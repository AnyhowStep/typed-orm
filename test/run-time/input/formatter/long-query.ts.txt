import * as tape from "tape";
import * as o from "../../../../dist/src/main";
import * as fs from "fs";

tape(__filename, (t) => {
    console.log(new o.SqlFormatter().format(
        fs.readFileSync(__dirname + "/long-query-sql.txt").toString()
    ));

    t.end();
});